import { createStore } from "vuex";
import {movie} from "./movie.module";

export default createStore({
    state: {},
    mutations: {},
    getters: {},
    computed: {},
    actions: {},
    modules: {
        movie
    },
});
